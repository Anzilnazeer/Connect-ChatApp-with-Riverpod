import 'package:flutter/material.dart';

class ConnectCalls extends StatelessWidget {
  const ConnectCalls({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
