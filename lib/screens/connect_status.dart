import 'package:flutter/material.dart';

class ConnectStatus extends StatelessWidget {
  const ConnectStatus({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
